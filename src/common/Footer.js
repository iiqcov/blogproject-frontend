import React from 'react';
import '../styles/Footer.css'; // CSS 파일을 import 합니다.

const Footer = () => {
    return (
        <footer className="footer">
        </footer>
    );
};

export default Footer;
